<%= simple_form_for(@menu_category) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :menu_id %>
    <%= f.input :category_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
